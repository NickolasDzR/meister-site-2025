mixin button(value = false)
    -
        var
            mod = value.type ? 'button_' + value.type : ''
            el = value.class ? value.class : ''
    +b.BUTTON(class=mod + ' ' + el)= value.text